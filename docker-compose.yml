version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: devops-heavy-project-app:local
    container_name: task-manager-api
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
      - PORT=80
    restart: unless-stopped
